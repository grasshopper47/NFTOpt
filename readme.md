To generate the types for the contract firstly run: `npm run clean` before compiling it.

There used to be magic here... now there is a duck.

```
      >(')
       )/
       /(
      /  `----/
      \  ~=- /
~~~^~^~^~^~^~^~^~^~~~
```
